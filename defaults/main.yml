python_required_libraries:
  - zlib1g-dev
  - libbz2-dev
  - libreadline6
  - libreadline6-dev
  - libsqlite3-dev
  - libssl-dev

python_pyenv_install_dir: "{{ anyenv_root | default('/opt/anyenv') }}/envs/pyenv"

python_version: 2.7.15
